<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.prima.projectlaravelpresales">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CustomerFormActivity" />
        <activity
            android:name=".MenuUtamaActivity"
            android:label="@string/title_activity_menu_utama"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".PresaleDetailActivity" />
        <activity android:name=".FormBuyerIdentificationActivity" />
        <activity android:name=".FormNamaStnkdanBpkbActivity" />
        <activity android:name=".FormUnitIdentification" />
        <activity android:name=".FormPresales" />
        <activity android:name=".FormUploadImage" />
        <activity android:name=".FormBuyerIdentificationSearching"></activity>
    </application>

</manifest>